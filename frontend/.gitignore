node_modules
dist
.tanstack

# Machine-local env overrides. The committed .env* files hold public SPA config
# only (the API base URL) — anything secret belongs here, or nowhere.
*.local

# Generated by `pnpm api:generate` from the backend's committed OpenAPI spec.
# A build artifact, not source — `dev`, `build` and `verify` all regenerate it.
src/generated

# Generated by the TanStack Router plugin from src/routes/**.
src/routeTree.gen.ts
