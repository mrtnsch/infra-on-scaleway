# Local development (`pnpm dev` on http://localhost:5173).
#
# The SPA calls the backend cross-origin, so the backend must allow this origin:
# add http://localhost:5173 to its CORS allow-list, or every request fails as an
# opaque network error rather than an HTTP status.
VITE_API_BASE_URL=http://localhost:8080
